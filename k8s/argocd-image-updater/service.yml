kind: Service
apiVersion: v1
metadata:
  name: app-service
  namespace: argocd
spec:
  selector:
    app: demo-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP

